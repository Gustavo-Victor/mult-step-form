import styled from "styled-components";

export const Info = styled.div`
   display: flex;
   flex-direction: column;
   gap: 1rem; 
   margin-bottom: 1rem; 

    p {
        font-size: 1.1rem; 
    }   

    strong {
        font-weight: 800;
        font-size: 1.2rem; 
        color: #fff;
    }
`;